# tglib (facade)

Re-exports `tglib-core`, `tglib-client`, and `tglib-tdlib`.

```rust
use tglib::{TelegramClient, TdlibAdapter, TelegramAccountId};
```

Enable live TDLib with feature `native` (= `tglib-tdlib/native`).

See the [workspace README](../../README.md).
